const styles = {
    Subtitle: ({ palette }) => ({
        color: palette.text.main
    }),
    Card: () => ({
        padding: "20px"
    }),
    Button: ({ palette }) => ({
        color: palette.text.white
    }),
}

export default styles